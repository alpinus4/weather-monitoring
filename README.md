# weather-monitoring

## Architecture 

![](diagram.png)

## Setup and running

```sh
docker compose up --build
```

`dotnet` cmdline tool version 7 has to be installed to run webapp exclusively:
```sh
cd webapp
dotnet run
```
